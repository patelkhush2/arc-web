{
  "name": "arc-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "studio": "npm run dev --prefix studio",
    "studio:deploy": "npm run deploy --prefix studio",
    "studio:seed": "npm run seed --prefix studio"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
